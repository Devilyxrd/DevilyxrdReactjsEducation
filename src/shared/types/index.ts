/**
 * Tüm feature'lar arası paylaşılan tipler.
 * Feature'a özgü tipler kendi feature klasöründe durur (örn. features/products/types.ts).
 */

export type Status = 'idle' | 'loading' | 'success' | 'error';
